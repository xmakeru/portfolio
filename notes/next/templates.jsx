// То же самое что layout, но оболочка при каждом переходе ремонтируется и обновляется, в отличие от layout, 
// т.е., например, состояние внутри template при переходе по страницам сбрасываются (в layout - нет), компоненты монтируются заново.

// Templates и layout можно использовать в тандеме.